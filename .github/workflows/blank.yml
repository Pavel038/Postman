name: Newman Run

on: [push, pull_request]

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v3
         - name: dfdsffew
           run: newman run  :"Trello_Pavel.postman_collection.json" --reporters cli   
     

  

