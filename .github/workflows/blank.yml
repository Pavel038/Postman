name: Newman Run

on: [push, pull_request]

jobs:
   build:

    runs-on: ubuntu-latest
  
  
    steps:
       - uses: actions/checkout@master
       - uses: matt-ball/newman-action@master
         with:
            collection: Trello_Pavel.postman_collection.json
            environment: Board.postman_environment.json
       
       - name: dfdsffew
         run: newman run  :"Trello_Pavel.postman_collection.json" --reporters cli   
     

  

