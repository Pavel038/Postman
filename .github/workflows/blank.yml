name: Newman Run

on: [push, pull_request]

  

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      -uses: newman run "C:\Users\Pavel\Desktop\Новая папка (3)\Trello_Pavel.postman_collection.json" -e "C:\Users\Pavel\Desktop\Новая папка (3)\Board.postman_environment.json"

        with:
          collection: postman_collection.json
